---
tags:
  - NOTE/forBook/Step-by-Step-Diffusion
  - CS/ML/DL/Diffusion
---
# 1 Fundamentals of Diffusion 扩散模型基础

![](000-attachments/Pasted%20image%2020250424162441.png)


生成模型的目标是：给定从某个未知分布 $p^∗(x)$ 中独立同分布（i.i.d.）地抽取的样本，构建一个采样器，用于（近似地）从同一个分布中采样。**例如**，给定一组狗的图像训练集，这些图像来自某个底层分布 $p_{dog}$​，我们希望有一种方法，能够从这个分布中生成新的狗图像

1. **目标分布 $p^*(x)$** ：这是一个未知的分布，我们希望通过生成模型来近似它。例如，如果我们有一个包含狗的图像的数据集，那么 $p^*(x)$  可能代表所有可能的狗的图像的分布。

2. **独立同分布（i.i.d.）样本**：这意味着我们从目标分布中随机抽取的样本是相互独立的，并且每个样本都遵循相同的分布。

3. **构建采样器**：生成模型的目标是构建一个采样器，这个采样器能够从目标分布中生成新的样本。换句话说，我们希望这个采样器能够产生与原始数据集相似的新数据。

4. **近似相同分布**：由于我们通常无法精确地知道目标分布 \( p^*(x) \)，生成模型的目标是构建一个采样器，它能够生成近似于 \( p^*(x) \) 分布的样本。

5. **狗图像的例子**：具体来说，如果我们有一个包含狗图像的训练集，这些图像是从某个底层分布 \( p_{dog} \) 中抽取的。我们的目标是开发一种方法，能够从这个底层分布 \( p_{dog} \) 中生成新的狗图像。


| 概念              | 解释                                     | 例子                                                                                  |
| --------------- | -------------------------------------- | ----------------------------------------------------------------------------------- |
| 目标分布 $p^*(x)$   | 一个未知的分布，我们希望通过生成模型来近似它                 | 如果有一个包含狗的图像的数据集，那么 $p^*(x)$  可能代表所有可能的狗的图像的分布——当然，我们还是不知道这个狗的分布是什么样子的，只是说这个分布应该是存在的 |
| 独立同分布（i.i.d.）样本 | 每个样本在概率上不会相互影响，且都遵循相同的分布               |                                                                                     |
| 构建采样器           | 生成模型的目标是构建一个采样器，这个采样器能够从目标分布中采样，生成新的样本 |                                                                                     |
| 近似相同分布          |                                        |                                                                                     |


---


![](000-attachments/Pasted%20image%2020250424162451.png)
在高层次上解决这个问题就是学习一个变换（transformation），用于实现如下转换：从一些*容易采样的分布（easy-to-sample distribution）*，比如高斯噪声分布，到我们的目标分布 $p^*$。扩散模型就提供了学习此类变换的一个通用的框架。它的聪明之处在于简化了问题——
- 原本：直接从目标分布$p^*$那里去采样
- 现在：从一些 *容易采样的分布（easy-to-sample distribution）* 进行采样，比如高斯噪声分布
说白了就是问题的简化

---

![](000-attachments/Pasted%20image%2020250424163110.png)

## 1.1 Gaussian Diffusion 高斯扩散

![](000-attachments/Pasted%20image%2020250424163229.png)

|                |         | 例如        |
| -------------- | ------- | --------- |
| $x_0$          | 随机变量    | 一张狗的图片    |
| $\mathbb{R}^d$ |         |           |
| $p^*$          | 目标分布    | 狗的图像服从的分布 |
| $\sigma$       | 高斯分布的方差 |           |


对于高斯扩散，设 $x_0$ 是一个在 $\mathbb{R}^d$ 中根据目标分布 $p^*$（例如，狗的图像）分布的随机变量。然后通过逐步添加一些具有小尺度  $\sigma$  的独立高斯噪声来构造一系列随机变量 $x_1, x_2, \ldots, x_T$ ：

$$
x_{t+1} := x_t + \eta_t, \quad \eta_t \sim \mathcal{N}(0, \sigma^2)   \tag{1}
$$

^87fe5e


这个“逐步添加噪声来构造一系列随机变量”的行为，就叫做前向过程（forward process）。这个过程将原始的数据分布变换到了噪声分布之中。[方程1](#^87fe5e) 就定义了



---


假设我们可以解决这个子问题：
> “给定一个边缘分布为 pt​ 的样本，生成一个[边缘分布](边缘分布.md)为 $p_t−1$​ 的样本”

如果我们可以解决这个子问题，那么解决这个子问题的方法，就叫做“逆向采样器”（reverse sampler），因为它告诉我们，如果能从分布 pt​ 哪儿采样，那该如何从 pt−1​ 采样。大胆点说，就是我们最终能够“直接”在分布 $p_0$ 中进行采样，条件就是从 pT​（通常是高斯分布）开始采样，然后迭代地应用逆向采样过程，逐步得到 pT−1​、pT−2​ 等，最终得到 p0​=p∗ 的样本。

![](000-attachments/Pasted%20image%2020250424173409.png)
所以，**扩散的关键见解是，学习逆转中的每一步，可能比一步到位地从目标分布p0中采样要容易**。这是因为相邻的分布 pt−1​ 和 pt​ 已经很接近，所以逆向采样器不需要做太多工作。有许多方法可以构建逆向采样器，但为了具体起见，我们首先看看标准的扩散采样器，即DDPM（Denoising Diffusion Probabilistic Models）采样器

> 接下来让我们进入DDPM（Denoising Diffusion Probabilistic Models）采样器

**理想DDPM采样器的策略**：在时间 $t$，给定输入 $z$（从 $p_t$ 中采样），我们从条件分布

$$
p(x_{t-1} \mid x_t = z).
$$

中输出一个样本

这个逆向采样器思路是对的，但是问题是，它需要为每个 $x_t$ 学习一个生成模型，用于条件分布 $p(x_{t-1} \mid x_t)$， $x_t$ 有很多个的话，生成模型就需要很多个，这可能就很复杂。但如果每一步的噪声 $\sigma$ 足够小，那么这个条件分布就会变得简单，我们可以更容易地从当前状态xt​推断出xt−1​。

---
![](000-attachments/Pasted%20image%2020250424175004.png)

> **事实 1**（扩散逆过程）。对于小的 $\sigma$ 和在（1）中定义的高斯扩散过程，条件分布 $p(x_{t-1} \mid x_t)$ 本身接近高斯分布[^1]。也就是说，对于所有的时间 $t$ 和条件 $z \in \mathbb{R}^d$，存在某个均值参数 $\mu \in \mathbb{R}^d$ 使得
> 
> $$
p(x_{t-1} \mid x_t = z) \approx \mathcal{N}(x_t; \mu, \sigma^2) \tag{3}
\>  $$


至于为什么“对于小的 $\sigma$ 和在（1）中定义的高斯扩散过程，条件分布 $p(x_{t-1} \mid x_t)$ 本身接近高斯分布”，这不是一个显而易见的事实；我们将在第 2.1 节中推导它。这个事实使得一个巨大的简化成为可能：我们不需要学习从零开始学习任意分布 $p(x_{t-1} \mid x_t)$，况且，我们现在除了它的均值之外[^2]，对这个分布一无所知，我们用 $\mu_{t-1}(x_t)$ 表示均值。

![](000-attachments/Pasted%20image%2020250424235418.png)
图 2 是事实 1 的说明。最左边的先验分布[^4] $p(x_{t-1})$ 定义了一个联合分布 $(x_{t-1}, x_t)$，其中 $p(x_t \mid x_{t-1}) = \mathcal{N}(0, \sigma^2)$。我们为一个固定的条件 $x_t$ 和变化的噪声水平 $\sigma$ 绘制了逆条件分布 $p(x_{t-1} \mid x_t)$。注意这些分布对于小的 $\sigma$ 变得接近高斯分布。


图 2 说明了当 σ 足够小时，我们可以将后验分布[^3]近似为高斯分布。这是一个重要的点，所以重申一下：对于给定的时间 $t$ 和条件值 $x_t$，学习 $p(x_{t-1} \mid x_t)$ 的均值就足以学习完整的条件分布 $p(x_{t-1} \mid x_t)$。


![](000-attachments/Pasted%20image%2020250425173031.png)

学习 $p(x_{t-1} \mid x_t)$ 的均值，相对于直接学习整个的条件分布，要简单得多，因为我们可以用回归来解决这个问题。To elaborate，我们有一个联合分布 $(x_{t-1}, x_t)$，我们能从里面很方便地进行采样。我们还想估计 $E[x_{t-1}, x_{t}]$[^5]。可以通过优化标准回归损失[^6]来实现：

$$
\mu_{t-1}(z) := \mathbb{E}[x_{t-1} \mid x_t = z] \tag{4}
$$

$$
\implies \mu_{t-1} = \arg\min_{f:\mathbb{R}^d \rightarrow \mathbb{R}^d} \mathbb{E}_{x_t, x_{t-1}} \|f(x_t) - x_{t-1}\|_2^2 \tag{5}
$$

$$
= \arg\min_{f:\mathbb{R}^d \rightarrow \mathbb{R}^d} \mathbb{E}_{x_{t-1} \sim \eta} \|f(x_{t-1} + \eta_t) - x_{t-1})\|_2^2 \tag{6}
$$
















[^1]: 所以可以用高斯分布来近似这些每个$x_t$都拥有的、各自不同的条件分布

[^2]: 为什么能知道它的均值？

[^3]: 在这里就是条件分布 $p(x_{t-1} \mid x_t)$ 

[^4]: [贝叶斯定理：先验与后验的桥梁](../../../../MATH/Probability%20Theory/贝叶斯定理：先验与后验的桥梁.md)

[^5]: [联合期望值](联合期望值.md)

[^6]: [常见的回归损失函数](常见的回归损失函数.md)
