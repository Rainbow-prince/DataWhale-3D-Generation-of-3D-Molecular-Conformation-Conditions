---
tags:
  - MATH/probability_theory
  - 
---


边缘分布（Marginal Distribution）是概率论和统计学中的一个概念，它描述的是在多维随机变量系统中，单个随机变量的概率分布情况，而不考虑与其他变量的联合分布。

考虑一个由多个随机变量组成的联合分布，例如 \( (X, Y) \)，其中 \( X \) 和 \( Y \) 都是随机变量。\( X \) 的边缘分布是指当我们忽略 \( Y \) 时 \( X \) 的分布情况，同样，\( Y \) 的边缘分布是指忽略 \( X \) 时 \( Y \) 的分布情况。数学上，边缘分布可以通过对联合分布进行积分或求和来获得，这取决于随机变量是连续的还是离散的。

### 对于连续随机变量

如果 \( f_{X,Y}(x,y) \) 是 \( X \) 和 \( Y \) 的联合概率密度函数，那么 \( X \) 的边缘概率密度函数 \( f_X(x) \) 可以通过对 \( Y \) 的所有可能值进行积分来获得：

\[ f_X(x) = \int_{-\infty}^{\infty} f_{X,Y}(x,y) \, dy \]

同样，\( Y \) 的边缘概率密度函数 \( f_Y(y) \) 可以通过对 \( X \) 的所有可能值进行积分来获得：

\[ f_Y(y) = \int_{-\infty}^{\infty} f_{X,Y}(x,y) \, dx \]

### 对于离散随机变量

如果 \( p_{X,Y}(x,y) \) 是 \( X \) 和 \( Y \) 的联合概率质量函数，那么 \( X \) 的边缘概率质量函数 \( p_X(x) \) 可以通过对 \( Y \) 的所有可能值求和来获得：

\[ p_X(x) = \sum_{y} p_{X,Y}(x,y) \]

同样，\( Y \) 的边缘概率质量函数 \( p_Y(y) \) 可以通过对 \( X \) 的所有可能值求和来获得：

\[ p_Y(y) = \sum_{x} p_{X,Y}(x,y) \]

在扩散模型的上下文中，边缘分布 \( p_t \) 通常指的是在扩散过程中的某个时间点 \( t \) 随机变量的分布，而不考虑它与其他时间点的联合分布。通过逐步逆转这个过程，我们可以从容易采样的分布（如高斯分布）生成目标分布的样本。