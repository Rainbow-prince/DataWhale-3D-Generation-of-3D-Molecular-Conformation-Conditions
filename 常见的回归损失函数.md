---
tags:
  - CS/ML
---


3. **均方误差（Mean Squared Error, MSE）**：
   均方误差计算预测值与真实值差的平方的平均值。其公式为：
   $$
   \text{MSE} = \frac{1}{n} \sum_{i=1}^{n}(y_i - \hat{y}_i)^2
   $$

4. **均方根误差（Root Mean Squared Error, RMSE）**：
   均方根误差是均方误差的平方根，其公式为：
   $$
   \text{RMSE} = \sqrt{\frac{1}{n} \sum_{i=1}^{n}(y_i - \hat{y}_i)^2}
   $$

5. **平均绝对误差（Mean Absolute Error, MAE）**：
   平均绝对误差计算预测值与真实值差的绝对值的平均值。其公式为：
   $$
   \text{MAE} = \frac{1}{n} \sum_{i=1}^{n}|y_i - \hat{y}_i|
   $$

6. **平均绝对百分比误差（Mean Absolute Percentage Error, MAPE）**：
   平均绝对百分比误差计算预测值与真实值差的绝对值占真实值的百分比的平均值。其公式为：
   $$
   \text{MAPE} = \frac{100\%}{n} \sum_{i=1}^{n}\left|\frac{y_i - \hat{y}_i}{y_i}\right|
   $$

7. **Huber损失**：
   Huber损失是一种结合了MSE和MAE优点的损失函数，它对小误差使用平方误差，对大误差使用绝对误差。其公式为：
   $$
   L(y, \hat{y}) = 
   \begin{cases} 
   \frac{1}{2}(y - \hat{y})^2 & \text{if } |y - \hat{y}| \leq \delta \\
   \delta |y - \hat{y}| - \frac{\delta^2}{2} & \text{otherwise}
   \end{cases}
   $$
   其中，$\delta$ 是一个超参数，用于控制损失函数从平方误差过渡到绝对误差的阈值。